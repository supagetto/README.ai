---
name: writing-typescript-code
description: >
  TypeScript/TSX style conventions. Consult when writing or editing
  TypeScript or TSX code.
paths:
  - '**/*.{ts,tsx}'
user-invocable: false
---

`${CLAUDE_SKILL_DIR}/rules` holds one file per convention, named after
the rule. List that directory, then read only the files relevant to the
code you're about to write or edit.
